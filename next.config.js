/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['avatars.githubusercontent.com', 'poblwebsite.azurewebsites.net', 'images.unsplash.com']
    }
}

module.exports = nextConfig
